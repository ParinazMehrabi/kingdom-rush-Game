package model.tower;

public class ArcherTower extends Tower{
    public ArcherTower(double destructionPower, double constructionCost, double range, double x, double y) {
        super(destructionPower, constructionCost, range, x, y);
    }
}
